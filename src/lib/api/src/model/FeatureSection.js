/**
 * Timesheet API
 * API for regions, clients, operations, tasks, timesheets, submissions & approvals
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The FeatureSection model module.
 * @module model/FeatureSection
 * @version 1.0.0
 */
class FeatureSection {
    /**
     * Constructs a new <code>FeatureSection</code>.
     * @alias module:model/FeatureSection
     * @param id {Number} 
     * @param title {String} 
     * @param description {String} 
     * @param features {Object} List of features as strings
     * @param image {String} 
     */
    constructor(id, title, description, features, image) { 
        
        FeatureSection.initialize(this, id, title, description, features, image);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, title, description, features, image) { 
        obj['id'] = id;
        obj['title'] = title;
        obj['description'] = description;
        obj['features'] = features;
        obj['image'] = image;
    }

    /**
     * Constructs a <code>FeatureSection</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FeatureSection} obj Optional instance to populate.
     * @return {module:model/FeatureSection} The populated <code>FeatureSection</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FeatureSection();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('features')) {
                obj['features'] = ApiClient.convertToType(data['features'], Object);
            }
            if (data.hasOwnProperty('image')) {
                obj['image'] = ApiClient.convertToType(data['image'], 'String');
            }
            if (data.hasOwnProperty('image_alt')) {
                obj['image_alt'] = ApiClient.convertToType(data['image_alt'], 'String');
            }
            if (data.hasOwnProperty('order')) {
                obj['order'] = ApiClient.convertToType(data['order'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FeatureSection</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FeatureSection</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FeatureSection.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['image'] && !(typeof data['image'] === 'string' || data['image'] instanceof String)) {
            throw new Error("Expected the field `image` to be a primitive type in the JSON string but got " + data['image']);
        }
        // ensure the json data is a string
        if (data['image_alt'] && !(typeof data['image_alt'] === 'string' || data['image_alt'] instanceof String)) {
            throw new Error("Expected the field `image_alt` to be a primitive type in the JSON string but got " + data['image_alt']);
        }

        return true;
    }


}

FeatureSection.RequiredProperties = ["id", "title", "description", "features", "image"];

/**
 * @member {Number} id
 */
FeatureSection.prototype['id'] = undefined;

/**
 * @member {String} title
 */
FeatureSection.prototype['title'] = undefined;

/**
 * @member {String} description
 */
FeatureSection.prototype['description'] = undefined;

/**
 * List of features as strings
 * @member {Object} features
 */
FeatureSection.prototype['features'] = undefined;

/**
 * @member {String} image
 */
FeatureSection.prototype['image'] = undefined;

/**
 * @member {String} image_alt
 */
FeatureSection.prototype['image_alt'] = undefined;

/**
 * Order in which sections appear
 * @member {Number} order
 */
FeatureSection.prototype['order'] = undefined;






export default FeatureSection;

