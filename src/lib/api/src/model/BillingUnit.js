/**
 * Timesheet API
 * API for regions, clients, operations, tasks, timesheets, submissions & approvals
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BillingUnit model module.
 * @module model/BillingUnit
 * @version 1.0.0
 */
class BillingUnit {
    /**
     * Constructs a new <code>BillingUnit</code>.
     * Minimal BillingUnit serializer for listing and admin usage. Serializador mínimo de BillingUnit para listado y uso admin.
     * @alias module:model/BillingUnit
     * @param id {Number} 
     * @param code {String} System identifier (immutable after creation)
     * @param label {String} Human-readable label (editable)
     */
    constructor(id, code, label) { 
        
        BillingUnit.initialize(this, id, code, label);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, code, label) { 
        obj['id'] = id;
        obj['code'] = code;
        obj['label'] = label;
    }

    /**
     * Constructs a <code>BillingUnit</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BillingUnit} obj Optional instance to populate.
     * @return {module:model/BillingUnit} The populated <code>BillingUnit</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BillingUnit();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'String');
            }
            if (data.hasOwnProperty('label')) {
                obj['label'] = ApiClient.convertToType(data['label'], 'String');
            }
            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BillingUnit</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BillingUnit</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BillingUnit.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['code'] && !(typeof data['code'] === 'string' || data['code'] instanceof String)) {
            throw new Error("Expected the field `code` to be a primitive type in the JSON string but got " + data['code']);
        }
        // ensure the json data is a string
        if (data['label'] && !(typeof data['label'] === 'string' || data['label'] instanceof String)) {
            throw new Error("Expected the field `label` to be a primitive type in the JSON string but got " + data['label']);
        }

        return true;
    }


}

BillingUnit.RequiredProperties = ["id", "code", "label"];

/**
 * @member {Number} id
 */
BillingUnit.prototype['id'] = undefined;

/**
 * System identifier (immutable after creation)
 * @member {String} code
 */
BillingUnit.prototype['code'] = undefined;

/**
 * Human-readable label (editable)
 * @member {String} label
 */
BillingUnit.prototype['label'] = undefined;

/**
 * Whether this billing unit is currently available
 * @member {Boolean} active
 */
BillingUnit.prototype['active'] = undefined;






export default BillingUnit;

