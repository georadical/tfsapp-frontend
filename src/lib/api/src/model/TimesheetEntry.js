/**
 * Timesheet API
 * API for regions, clients, operations, tasks, timesheets, submissions & approvals
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ProgressEnum from './ProgressEnum';
import Status300Enum from './Status300Enum';

/**
 * The TimesheetEntry model module.
 * @module model/TimesheetEntry
 * @version 1.0.0
 */
class TimesheetEntry {
    /**
     * Constructs a new <code>TimesheetEntry</code>.
     * Serializer for TimesheetEntry; computed fields are read-only. Serializador para TimesheetEntry; campos calculados son de solo lectura.
     * @alias module:model/TimesheetEntry
     * @param id {Number} 
     * @param employee {Number} 
     * @param client {Number} 
     * @param region {Number} 
     * @param operation {Number} 
     * @param date {Date} 
     * @param startTime {String} 
     * @param finishTime {String} 
     * @param hours {Number} 
     * @param rateApplied {Number} 
     * @param income {Number} 
     */
    constructor(id, employee, client, region, operation, date, startTime, finishTime, hours, rateApplied, income) { 
        
        TimesheetEntry.initialize(this, id, employee, client, region, operation, date, startTime, finishTime, hours, rateApplied, income);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, employee, client, region, operation, date, startTime, finishTime, hours, rateApplied, income) { 
        obj['id'] = id;
        obj['employee'] = employee;
        obj['client'] = client;
        obj['region'] = region;
        obj['operation'] = operation;
        obj['date'] = date;
        obj['start_time'] = startTime;
        obj['finish_time'] = finishTime;
        obj['hours'] = hours;
        obj['rate_applied'] = rateApplied;
        obj['income'] = income;
    }

    /**
     * Constructs a <code>TimesheetEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TimesheetEntry} obj Optional instance to populate.
     * @return {module:model/TimesheetEntry} The populated <code>TimesheetEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TimesheetEntry();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('employee')) {
                obj['employee'] = ApiClient.convertToType(data['employee'], 'Number');
            }
            if (data.hasOwnProperty('client')) {
                obj['client'] = ApiClient.convertToType(data['client'], 'Number');
            }
            if (data.hasOwnProperty('region')) {
                obj['region'] = ApiClient.convertToType(data['region'], 'Number');
            }
            if (data.hasOwnProperty('operation')) {
                obj['operation'] = ApiClient.convertToType(data['operation'], 'Number');
            }
            if (data.hasOwnProperty('task')) {
                obj['task'] = ApiClient.convertToType(data['task'], 'Number');
            }
            if (data.hasOwnProperty('work_order')) {
                obj['work_order'] = ApiClient.convertToType(data['work_order'], 'Number');
            }
            if (data.hasOwnProperty('date')) {
                obj['date'] = ApiClient.convertToType(data['date'], 'Date');
            }
            if (data.hasOwnProperty('start_time')) {
                obj['start_time'] = ApiClient.convertToType(data['start_time'], 'String');
            }
            if (data.hasOwnProperty('finish_time')) {
                obj['finish_time'] = ApiClient.convertToType(data['finish_time'], 'String');
            }
            if (data.hasOwnProperty('hours')) {
                obj['hours'] = ApiClient.convertToType(data['hours'], 'Number');
            }
            if (data.hasOwnProperty('done')) {
                obj['done'] = ApiClient.convertToType(data['done'], 'Number');
            }
            if (data.hasOwnProperty('rate_applied')) {
                obj['rate_applied'] = ApiClient.convertToType(data['rate_applied'], 'Number');
            }
            if (data.hasOwnProperty('income')) {
                obj['income'] = ApiClient.convertToType(data['income'], 'Number');
            }
            if (data.hasOwnProperty('progress')) {
                obj['progress'] = ProgressEnum.constructFromObject(data['progress']);
            }
            if (data.hasOwnProperty('risk_assessment')) {
                obj['risk_assessment'] = ApiClient.convertToType(data['risk_assessment'], 'Boolean');
            }
            if (data.hasOwnProperty('hazard_report')) {
                obj['hazard_report'] = ApiClient.convertToType(data['hazard_report'], 'Boolean');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = Status300Enum.constructFromObject(data['status']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TimesheetEntry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TimesheetEntry</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TimesheetEntry.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['start_time'] && !(typeof data['start_time'] === 'string' || data['start_time'] instanceof String)) {
            throw new Error("Expected the field `start_time` to be a primitive type in the JSON string but got " + data['start_time']);
        }
        // ensure the json data is a string
        if (data['finish_time'] && !(typeof data['finish_time'] === 'string' || data['finish_time'] instanceof String)) {
            throw new Error("Expected the field `finish_time` to be a primitive type in the JSON string but got " + data['finish_time']);
        }
        // ensure the json data is a string
        if (data['hours'] && !(typeof data['hours'] === 'string' || data['hours'] instanceof String)) {
            throw new Error("Expected the field `hours` to be a primitive type in the JSON string but got " + data['hours']);
        }
        // ensure the json data is a string
        if (data['done'] && !(typeof data['done'] === 'string' || data['done'] instanceof String)) {
            throw new Error("Expected the field `done` to be a primitive type in the JSON string but got " + data['done']);
        }
        // ensure the json data is a string
        if (data['rate_applied'] && !(typeof data['rate_applied'] === 'string' || data['rate_applied'] instanceof String)) {
            throw new Error("Expected the field `rate_applied` to be a primitive type in the JSON string but got " + data['rate_applied']);
        }
        // ensure the json data is a string
        if (data['income'] && !(typeof data['income'] === 'string' || data['income'] instanceof String)) {
            throw new Error("Expected the field `income` to be a primitive type in the JSON string but got " + data['income']);
        }
        // ensure the json data is a string
        if (data['notes'] && !(typeof data['notes'] === 'string' || data['notes'] instanceof String)) {
            throw new Error("Expected the field `notes` to be a primitive type in the JSON string but got " + data['notes']);
        }

        return true;
    }


}

TimesheetEntry.RequiredProperties = ["id", "employee", "client", "region", "operation", "date", "start_time", "finish_time", "hours", "rate_applied", "income"];

/**
 * @member {Number} id
 */
TimesheetEntry.prototype['id'] = undefined;

/**
 * @member {Number} employee
 */
TimesheetEntry.prototype['employee'] = undefined;

/**
 * @member {Number} client
 */
TimesheetEntry.prototype['client'] = undefined;

/**
 * @member {Number} region
 */
TimesheetEntry.prototype['region'] = undefined;

/**
 * @member {Number} operation
 */
TimesheetEntry.prototype['operation'] = undefined;

/**
 * @member {Number} task
 */
TimesheetEntry.prototype['task'] = undefined;

/**
 * @member {Number} work_order
 */
TimesheetEntry.prototype['work_order'] = undefined;

/**
 * @member {Date} date
 */
TimesheetEntry.prototype['date'] = undefined;

/**
 * @member {String} start_time
 */
TimesheetEntry.prototype['start_time'] = undefined;

/**
 * @member {String} finish_time
 */
TimesheetEntry.prototype['finish_time'] = undefined;

/**
 * @member {Number} hours
 */
TimesheetEntry.prototype['hours'] = undefined;

/**
 * @member {Number} done
 */
TimesheetEntry.prototype['done'] = undefined;

/**
 * @member {Number} rate_applied
 */
TimesheetEntry.prototype['rate_applied'] = undefined;

/**
 * @member {Number} income
 */
TimesheetEntry.prototype['income'] = undefined;

/**
 * @member {module:model/ProgressEnum} progress
 */
TimesheetEntry.prototype['progress'] = undefined;

/**
 * @member {Boolean} risk_assessment
 */
TimesheetEntry.prototype['risk_assessment'] = undefined;

/**
 * @member {Boolean} hazard_report
 */
TimesheetEntry.prototype['hazard_report'] = undefined;

/**
 * @member {String} notes
 */
TimesheetEntry.prototype['notes'] = undefined;

/**
 * @member {module:model/Status300Enum} status
 */
TimesheetEntry.prototype['status'] = undefined;






export default TimesheetEntry;

