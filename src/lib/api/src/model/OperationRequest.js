/**
 * Timesheet API
 * API for regions, clients, operations, tasks, timesheets, submissions & approvals
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OperationStatusEnum from './OperationStatusEnum';

/**
 * The OperationRequest model module.
 * @module model/OperationRequest
 * @version 1.0.0
 */
class OperationRequest {
    /**
     * Constructs a new <code>OperationRequest</code>.
     * Minimal Operation serializer. Serializador mínimo de Operation.
     * @alias module:model/OperationRequest
     * @param name {String} 
     * @param client {Number} 
     * @param region {Number} 
     * @param billingUnit {Number} 
     */
    constructor(name, client, region, billingUnit) { 
        
        OperationRequest.initialize(this, name, client, region, billingUnit);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, client, region, billingUnit) { 
        obj['name'] = name;
        obj['client'] = client;
        obj['region'] = region;
        obj['billing_unit'] = billingUnit;
    }

    /**
     * Constructs a <code>OperationRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OperationRequest} obj Optional instance to populate.
     * @return {module:model/OperationRequest} The populated <code>OperationRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OperationRequest();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('client')) {
                obj['client'] = ApiClient.convertToType(data['client'], 'Number');
            }
            if (data.hasOwnProperty('region')) {
                obj['region'] = ApiClient.convertToType(data['region'], 'Number');
            }
            if (data.hasOwnProperty('billing_unit')) {
                obj['billing_unit'] = ApiClient.convertToType(data['billing_unit'], 'Number');
            }
            if (data.hasOwnProperty('coupe_no')) {
                obj['coupe_no'] = ApiClient.convertToType(data['coupe_no'], 'String');
            }
            if (data.hasOwnProperty('id_info')) {
                obj['id_info'] = ApiClient.convertToType(data['id_info'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = OperationStatusEnum.constructFromObject(data['status']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OperationRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OperationRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OperationRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['coupe_no'] && !(typeof data['coupe_no'] === 'string' || data['coupe_no'] instanceof String)) {
            throw new Error("Expected the field `coupe_no` to be a primitive type in the JSON string but got " + data['coupe_no']);
        }
        // ensure the json data is a string
        if (data['id_info'] && !(typeof data['id_info'] === 'string' || data['id_info'] instanceof String)) {
            throw new Error("Expected the field `id_info` to be a primitive type in the JSON string but got " + data['id_info']);
        }

        return true;
    }


}

OperationRequest.RequiredProperties = ["name", "client", "region", "billing_unit"];

/**
 * @member {String} name
 */
OperationRequest.prototype['name'] = undefined;

/**
 * @member {Number} client
 */
OperationRequest.prototype['client'] = undefined;

/**
 * @member {Number} region
 */
OperationRequest.prototype['region'] = undefined;

/**
 * @member {Number} billing_unit
 */
OperationRequest.prototype['billing_unit'] = undefined;

/**
 * @member {String} coupe_no
 */
OperationRequest.prototype['coupe_no'] = undefined;

/**
 * @member {String} id_info
 */
OperationRequest.prototype['id_info'] = undefined;

/**
 * @member {module:model/OperationStatusEnum} status
 */
OperationRequest.prototype['status'] = undefined;






export default OperationRequest;

