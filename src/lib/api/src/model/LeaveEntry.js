/**
 * Timesheet API
 * API for regions, clients, operations, tasks, timesheets, submissions & approvals
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import LeaveTypeEnum from './LeaveTypeEnum';

/**
 * The LeaveEntry model module.
 * @module model/LeaveEntry
 * @version 1.0.0
 */
class LeaveEntry {
    /**
     * Constructs a new <code>LeaveEntry</code>.
     * Serializer for LeaveEntry to expose CRUD fields to the API. Serializador para LeaveEntry que expone campos CRUD al API.
     * @alias module:model/LeaveEntry
     * @param id {Number} 
     * @param startDate {Date} 
     * @param leaveType {module:model/LeaveTypeEnum} 
     * @param createdAt {Date} 
     * @param updatedAt {Date} 
     */
    constructor(id, startDate, leaveType, createdAt, updatedAt) { 
        
        LeaveEntry.initialize(this, id, startDate, leaveType, createdAt, updatedAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, startDate, leaveType, createdAt, updatedAt) { 
        obj['id'] = id;
        obj['start_date'] = startDate;
        obj['leave_type'] = leaveType;
        obj['created_at'] = createdAt;
        obj['updated_at'] = updatedAt;
    }

    /**
     * Constructs a <code>LeaveEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LeaveEntry} obj Optional instance to populate.
     * @return {module:model/LeaveEntry} The populated <code>LeaveEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LeaveEntry();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('employee')) {
                obj['employee'] = ApiClient.convertToType(data['employee'], 'Number');
            }
            if (data.hasOwnProperty('start_date')) {
                obj['start_date'] = ApiClient.convertToType(data['start_date'], 'Date');
            }
            if (data.hasOwnProperty('end_date')) {
                obj['end_date'] = ApiClient.convertToType(data['end_date'], 'Date');
            }
            if (data.hasOwnProperty('leave_type')) {
                obj['leave_type'] = LeaveTypeEnum.constructFromObject(data['leave_type']);
            }
            if (data.hasOwnProperty('hours_or_days')) {
                obj['hours_or_days'] = ApiClient.convertToType(data['hours_or_days'], 'Number');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LeaveEntry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LeaveEntry</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LeaveEntry.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['hours_or_days'] && !(typeof data['hours_or_days'] === 'string' || data['hours_or_days'] instanceof String)) {
            throw new Error("Expected the field `hours_or_days` to be a primitive type in the JSON string but got " + data['hours_or_days']);
        }
        // ensure the json data is a string
        if (data['notes'] && !(typeof data['notes'] === 'string' || data['notes'] instanceof String)) {
            throw new Error("Expected the field `notes` to be a primitive type in the JSON string but got " + data['notes']);
        }

        return true;
    }


}

LeaveEntry.RequiredProperties = ["id", "start_date", "leave_type", "created_at", "updated_at"];

/**
 * @member {Number} id
 */
LeaveEntry.prototype['id'] = undefined;

/**
 * @member {Number} employee
 */
LeaveEntry.prototype['employee'] = undefined;

/**
 * @member {Date} start_date
 */
LeaveEntry.prototype['start_date'] = undefined;

/**
 * @member {Date} end_date
 */
LeaveEntry.prototype['end_date'] = undefined;

/**
 * @member {module:model/LeaveTypeEnum} leave_type
 */
LeaveEntry.prototype['leave_type'] = undefined;

/**
 * EN: Optional quantity of leave taken (hours or days). ES: Cantidad opcional de licencia tomada (horas o días).
 * @member {Number} hours_or_days
 */
LeaveEntry.prototype['hours_or_days'] = undefined;

/**
 * @member {String} notes
 */
LeaveEntry.prototype['notes'] = undefined;

/**
 * @member {Date} created_at
 */
LeaveEntry.prototype['created_at'] = undefined;

/**
 * @member {Date} updated_at
 */
LeaveEntry.prototype['updated_at'] = undefined;






export default LeaveEntry;

